package com.internship.financeservice.utils.validation

import java.math.BigDecimal

interface Constants {
    companion object {
        val SALARY_PERCENT: BigDecimal = BigDecimal.valueOf(0.5)
    }
}